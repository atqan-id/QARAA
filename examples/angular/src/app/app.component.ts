import { Component } from '@angular/core'; import { createLocalSession } from '@atqan/qaraa-client'; import { QaraaSessionService } from '@atqan/qaraa-angular'; import { exampleCorpus } from '../../../shared/src/fixture.ts'; export function connectExample(service:QaraaSessionService){service.connect(createLocalSession({corpus:exampleCorpus}));} @Component({standalone:true,selector:'qaraa-example',template:'<output>{{ service.snapshot()?.revision ?? 0 }}</output>',providers:[QaraaSessionService]}) export class AppComponent { constructor(readonly service:QaraaSessionService){connectExample(service);} }
