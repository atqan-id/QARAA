import { mount } from 'svelte'; import { createLocalSession } from '@atqan/qaraa-client'; import { createQaraaStore } from '@atqan/qaraa-svelte'; import { exampleCorpus } from '../../shared/src/fixture.ts'; import App from './App.svelte'; export const exampleStore=()=>createQaraaStore(createLocalSession({corpus:exampleCorpus})); if(typeof document!=='undefined') mount(App,{target:document.getElementById('app')!});
