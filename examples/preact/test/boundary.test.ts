import assert from 'node:assert/strict'; import test from 'node:test'; import { h, render } from 'preact'; import { act } from 'preact/test-utils'; import { Window } from 'happy-dom';
import { createTrackedExampleSession } from '../../shared/src/fixture.ts'; import { verifyMountedExample } from '../../shared/src/verify.ts'; import { useQaraaSession } from '../../../packages/preact/src/index.ts';
test('Preact example mounts a real stale-safe session and tears down',async()=>{const browser=new Window({url:'https://example.test'});Object.assign(globalThis,{window:browser,document:browser.document});const host=browser.document.createElement('div');const tracked=createTrackedExampleSession();let revision:number|null=null;function Probe(){const q=useQaraaSession(tracked.session);revision=q.snapshot.revision;return null;}act(()=>render(h(Probe),host));await verifyMountedExample(tracked.session,()=>revision,async()=>act(async()=>undefined));act(()=>render(null,host));await tracked.session.close();assert.deepEqual(tracked.stats,{subscriptions:1,unsubscriptions:1,closes:1});browser.close();});
