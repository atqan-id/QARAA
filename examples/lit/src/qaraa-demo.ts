import { LitElement, html, type ReactiveControllerHost } from 'lit'; import { createLocalSession } from '@atqan/qaraa-client'; import { QaraaSessionController } from '@atqan/qaraa-lit'; import { exampleCorpus } from '../../shared/src/fixture.ts'; export function attachExample(host:ReactiveControllerHost){return new QaraaSessionController(host,createLocalSession({corpus:exampleCorpus}));} class QaraaDemo extends LitElement { private readonly qaraa=attachExample(this); render(){return html`<output>${this.qaraa.snapshot?.revision ?? 0}</output>`;} } customElements.define('qaraa-demo',QaraaDemo);
